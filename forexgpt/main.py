from discord_bot import ForexAnneBot

if __name__ == "__main__":
    # Example of creating and running the bot instance
    bot_instance_test = ForexAnneBot(test_mode=True)
    bot_instance_test.run()
    # bot_instance_live = ForexAnneBot(test_mode=False)
    # bot_instance_live.run()
